api_version: v1
plugin_name: channel-slack
plugin_version: 0.1.0
display_name: Slack Channel Plugin
category: channel
entrypoints:
  invoke: stdio-json
compatibility:
  runner_api: v1
capabilities:
  - slack.send
  - slack.approval
security:
  internal_auth: required
  provider_secret: required
